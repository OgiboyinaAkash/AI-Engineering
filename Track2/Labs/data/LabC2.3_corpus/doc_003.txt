Error recovery should classify failures, retry intelligently, and re-plan when output is malformed. Agents must log decisions and enforce permission boundaries. Temporary failures like timeouts should trigger a retry with exponential backoff. Permanent failures like authentication errors should escalate to the user rather than retrying. A well-designed agent stops safely and reports its state rather than entering an infinite retry loop.