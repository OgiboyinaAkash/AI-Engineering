AI agents are systems that combine reasoning and tool use. They follow the ReAct pattern: alternate thought and action while using tools such as search and email APIs. Agents are distinct from simple chains because they decide dynamically which tools to use based on what they observe. A chain executes a fixed sequence of steps defined before runtime; an agent chooses its next step after seeing the result of the previous one.