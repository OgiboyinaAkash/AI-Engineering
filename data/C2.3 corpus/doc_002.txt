Tool use in AI agents requires structured schemas that define the function name, description, and parameter types. The LLM reads tool descriptions to select the appropriate tool for each step. Multi-tool agents can combine search, summarize, and save operations to complete complex tasks. Tool outputs must be validated before the agent continues reasoning. A tool schema is a JSON object that specifies required and optional parameters, allowing the model to validate arguments before calling the function.